From: L. David Baron <dbaron@dbaron.org>

Fix -Wshadow warning in nsHtml5ByteReadable.h.

diff --git a/parser/html/nsHtml5ByteReadable.h b/parser/html/nsHtml5ByteReadable.h
--- a/parser/html/nsHtml5ByteReadable.h
+++ b/parser/html/nsHtml5ByteReadable.h
@@ -42,19 +42,19 @@
 
 /**
  * A weak reference wrapper around a byte array.
  */
 class nsHtml5ByteReadable
 {
   public:
 
-    nsHtml5ByteReadable(const PRUint8* current, const PRUint8* end)
-     : current(current),
-       end(end)
+    nsHtml5ByteReadable(const PRUint8* aCurrent, const PRUint8* aEnd)
+     : current(aCurrent),
+       end(aEnd)
     {
     }
 
     inline PRInt32 read() {
       if (current < end) {
         return *(current++);
       } else {
         return -1;
