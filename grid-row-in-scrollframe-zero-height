Make grid rows in a scroll frame not contribute to min-height.  b=413027

diff --git a/layout/xul/base/src/grid/nsGrid.cpp b/layout/xul/base/src/grid/nsGrid.cpp
--- a/layout/xul/base/src/grid/nsGrid.cpp
+++ b/layout/xul/base/src/grid/nsGrid.cpp
@@ -960,7 +960,7 @@ nsGrid::GetMinRowHeight(nsBoxLayoutState
 
   nsGridRow* row = GetRowAt(aIndex, aIsHorizontal);
 
-  if (row->IsCollapsed(aState))
+  if (row->IsCollapsed(aState) || row->mInScrollFrame)
     return 0;
 
   if (row->IsMinSet()) 
