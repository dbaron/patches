Avoid "/builds/trunk/obj/firefox-debugopt/config/nsinstall: cannot create /builds/trunk/obj/firefox-debugopt/dist/bin/firefox-bin: Text file busy" when running the app by always doing the unlink/open pair rather than only doing it some of the time.

diff --git a/config/nsinstall.c b/config/nsinstall.c
--- a/config/nsinstall.c
+++ b/config/nsinstall.c
@@ -174,7 +174,7 @@ copyfile( char *name, char *toname, mode
   fromfd = open(name, O_RDONLY);
   if (fromfd < 0 || fstat(fromfd, &sb) < 0)
     fail("cannot access %s", name);
-  if (exists && (!S_ISREG(tosb.st_mode) || access(toname, W_OK) < 0))
+  if (exists)
     (void) (S_ISDIR(tosb.st_mode) ? rmdir : unlink)(toname);
   tofd = open(toname, O_CREAT | O_WRONLY, 0666);
   if (tofd < 0)
