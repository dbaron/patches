Call wasFreed in BindToTree so elements that are reinserted after being removed are no longer in the should-be-freed list for cycle collector debugging (and causing false positive warnings).  b=420548

diff --git a/content/base/src/nsGenericElement.cpp b/content/base/src/nsGenericElement.cpp
--- a/content/base/src/nsGenericElement.cpp
+++ b/content/base/src/nsGenericElement.cpp
@@ -2170,6 +2170,12 @@ nsGenericElement::BindToTree(nsIDocument
   NS_POSTCONDITION(aBindingParent == GetBindingParent(),
                    "Bound to wrong binding parent");
 
+#ifdef DEBUG
+  // It wasn't actually freed, but we don't want it in the
+  // should-be-freed list anymore.
+  nsCycleCollector_DEBUG_wasFreed(static_cast<nsINode*>(this));
+#endif
+
   return NS_OK;
 }
 
