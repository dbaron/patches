From: L. David Baron <dbaron@dbaron.org>
Properly print parentheses in CSS values in debug List method.

diff --git a/layout/style/Declaration.cpp b/layout/style/Declaration.cpp
--- a/layout/style/Declaration.cpp
+++ b/layout/style/Declaration.cpp
@@ -1406,17 +1406,17 @@ Declaration::List(FILE* out, int32_t aIn
 {
   for (int32_t index = aIndent; --index >= 0; ) {
     fprintf_stderr(out, "  ");
   }
 
   fprintf_stderr(out, "{ ");
   nsAutoString s;
   ToString(s);
-  fprintf_stderr(out, NS_ConvertUTF16toUTF8(s).get());
+  fprintf_stderr(out, "%s", NS_ConvertUTF16toUTF8(s).get());
   fprintf_stderr(out, "}");
 }
 #endif
 
 bool
 Declaration::GetNthProperty(uint32_t aIndex, nsAString& aReturn) const
 {
   aReturn.Truncate();
