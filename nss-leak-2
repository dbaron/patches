Add a fix for another NSS leak by copying code from token_destructor (why don't all calls to nssToken_Destroy need this?).

diff --git a/security/nss/lib/pki/pki3hack.c b/security/nss/lib/pki/pki3hack.c
--- a/security/nss/lib/pki/pki3hack.c
+++ b/security/nss/lib/pki/pki3hack.c
@@ -212,6 +212,8 @@ STAN_RemoveModuleFromDefaultTrustDomain 
 	    nssToken_NotifyCertsNotVisible(token);
 	    nssList_Remove(td->tokenList, token);
 	    PK11Slot_SetNSSToken(module->slots[i], NULL);
+	    /* in 3.4, also destroy the slot (managed separately) */
+	    (void)nssSlot_Destroy(tok->slot);
 	    nssToken_Destroy(token);
  	}
     }
