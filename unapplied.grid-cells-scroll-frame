Make grid cells know whether they're in a scroll frame.  (Bug 413027)

diff --git a/layout/xul/base/src/grid/nsGrid.cpp b/layout/xul/base/src/grid/nsGrid.cpp
--- a/layout/xul/base/src/grid/nsGrid.cpp
+++ b/layout/xul/base/src/grid/nsGrid.cpp
@@ -429,8 +429,8 @@ nsGrid::BuildCellMap(PRInt32 aRows, PRIn
       // clear out cellmap
       for (PRInt32 i=0; i < oldsize; i++)
       {
-        mCellMap[i].SetBoxInRow(nsnull);
-        mCellMap[i].SetBoxInColumn(nsnull);
+        mCellMap[i].SetBoxInRow(nsnull, PR_FALSE);
+        mCellMap[i].SetBoxInColumn(nsnull, PR_FALSE);
       }
       return mCellMap;
     }
@@ -477,9 +477,9 @@ nsGrid::PopulateCellMap(nsGridRow* aRows
          }
 
          if (aIsHorizontal)
-           GetCellAt(j,i)->SetBoxInRow(child);
+           GetCellAt(j,i)->SetBoxInRow(child, row->mInScrollFrame);
          else
-           GetCellAt(i,j)->SetBoxInColumn(child);
+           GetCellAt(i,j)->SetBoxInColumn(child, row->mInScrollFrame);
 
          child = child->GetNextBox();
 
diff --git a/layout/xul/base/src/grid/nsGridCell.h b/layout/xul/base/src/grid/nsGridCell.h
--- a/layout/xul/base/src/grid/nsGridCell.h
+++ b/layout/xul/base/src/grid/nsGridCell.h
@@ -72,12 +72,15 @@ public:
 // accessors
     nsIBox*     GetBoxInColumn()             { return mBoxInColumn; }
     nsIBox*     GetBoxInRow()                { return mBoxInRow; }
-    void        SetBoxInRow(nsIBox* aBox)    { mBoxInRow = aBox; }
-    void        SetBoxInColumn(nsIBox* aBox) { mBoxInColumn = aBox; }
+    void        SetBoxInRow(nsIBox* aBox, PRBool aRowInScrollFrame)
+      { mBoxInRow = aBox; mRowInScrollFrame = aRowInScrollFrame; }
+    void        SetBoxInColumn(nsIBox* aBox, PRBool aColumnInScrollFrame)
+      { mBoxInColumn = aBox; mColumnInScrollFrame = aColumnInScrollFrame; }
 
 private:
     nsIBox* mBoxInColumn;
     nsIBox* mBoxInRow;
+    PRPackedBool mRowInScrollFrame, mColumnInScrollFrame;
 };
 
 #endif
