From: L. David Baron <dbaron@dbaron.org>

Bug 1041075 - Request longer timeout for test_value_storage.html.

I think this has become needed due to changes in the configuration of
the VMs on which we're running tests.

diff --git a/layout/style/test/test_value_storage.html b/layout/style/test/test_value_storage.html
--- a/layout/style/test/test_value_storage.html
+++ b/layout/style/test/test_value_storage.html
@@ -283,16 +283,16 @@ function runTest() {
     }
     test_property(props.pop());
     SimpleTest.executeSoon(do_one);
   }
   SimpleTest.executeSoon(do_one);
 }
 
 SimpleTest.waitForExplicitFinish();
-SimpleTest.requestLongerTimeout(2);
+SimpleTest.requestLongerTimeout(3);
 
 SpecialPowers.pushPrefEnv({ set: [["layout.css.variables.enabled", true]] },
                           runTest);
 </script>
 </pre>
 </body>
 </html>
