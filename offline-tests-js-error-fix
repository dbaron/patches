From: L. David Baron <dbaron@dbaron.org>

Fix JS error in offline tests resulting from incrementing gStep after the window is closed.

diff --git a/dom/tests/mochitest/ajax/offline/test_fallback.html b/dom/tests/mochitest/ajax/offline/test_fallback.html
--- a/dom/tests/mochitest/ajax/offline/test_fallback.html
+++ b/dom/tests/mochitest/ajax/offline/test_fallback.html
@@ -94,16 +94,17 @@ function onFallbackLoad(fallbackIdentifi
       window.clearTimeout(gCompleteTimeout);
       gCompleteTimeout = null;
 
       break;
   }
 
   if (gStep == 105) {
     finalize();
+    return;
   }
 
   ++gStep;
 }
 
 function finalize()
 {
   netscape.security.PrivilegeManager.enablePrivilege("UniversalXPConnect");
