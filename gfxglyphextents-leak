From: L. David Baron <dbaron@dbaron.org>

Fix bad leak statistics for gfxGlyphExtents.  (Bug 435138)

diff --git a/gfx/thebes/gfxFont.h b/gfx/thebes/gfxFont.h
--- a/gfx/thebes/gfxFont.h
+++ b/gfx/thebes/gfxFont.h
@@ -1289,16 +1289,22 @@ private:
         }
 
         nsTArray<uintptr_t> mBlocks;
     };
 
     GlyphWidths             mContainedGlyphWidths;
     nsTHashtable<HashEntry> mTightGlyphExtents;
     int32_t                 mAppUnitsPerDevUnit;
+
+    private:
+    // Declare an UNIMPLEMENTED copy-constructor and assignment operator as
+    // private to prevent accidental use of the default ones.
+    gfxGlyphExtents(const gfxGlyphExtents& aOther);
+    gfxGlyphExtents& operator=(const gfxGlyphExtents& aOther);
 };
 
 /**
  * gfxFontShaper
  *
  * This class implements text shaping (character to glyph mapping and
  * glyph layout). There is a gfxFontShaper subclass for each text layout
  * technology (uniscribe, core text, harfbuzz,....) we support.
