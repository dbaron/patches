Fix bad leak statistics for gfxGlyphExtents.  b=435138

diff --git a/gfx/thebes/public/gfxFont.h b/gfx/thebes/public/gfxFont.h
--- a/gfx/thebes/public/gfxFont.h
+++ b/gfx/thebes/public/gfxFont.h
@@ -351,16 +351,22 @@ private:
         }
 
         nsTArray<PtrBits> mBlocks;
     };
     
     GlyphWidths             mContainedGlyphWidths;
     nsTHashtable<HashEntry> mTightGlyphExtents;
     PRUint32                mAppUnitsPerDevUnit;
+
+    private:
+    // Declare an UNIMPLEMENTED copy-constructor and assignment operator as
+    // private to prevent accidental use of the default ones.
+    gfxGlyphExtents(const gfxGlyphExtents& aOther);
+    gfxGlyphExtents& operator=(const gfxGlyphExtents& aOther);
 };
 
 /* a SPECIFIC single font family */
 class THEBES_API gfxFont {
 public:
     nsrefcnt AddRef(void) {
         NS_PRECONDITION(PRInt32(mRefCnt) >= 0, "illegal refcnt");
         if (mExpirationState.IsTracked()) {
