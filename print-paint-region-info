Print region info about NS_PAINT events.

diff --git a/view/src/nsViewManager.cpp b/view/src/nsViewManager.cpp
--- a/view/src/nsViewManager.cpp
+++ b/view/src/nsViewManager.cpp
@@ -1024,16 +1024,35 @@ NS_IMETHODIMP nsViewManager::DispatchEve
         nsCOMPtr<nsIRegion> region = event->region;
         if (!region) {
           if (NS_FAILED(CreateRegion(getter_AddRefs(region))))
             break;
 
           const nsIntRect& damrect = *event->rect;
           region->SetTo(damrect.x, damrect.y, damrect.width, damrect.height);
         }
+
+#if 0
+        {
+          nsRegionRectSet *rects = nsnull;
+          region->GetRects(&rects);
+
+          PRTime now = PR_Now();
+          printf("Got NS_PAINT event at %u.%u with region containing rectangles:\n",
+                 PRUint32(now / PR_USEC_PER_SEC),
+                 PRUint32(now % PR_USEC_PER_SEC));
+          for (PRUint32 i = 0; i < rects->mNumRects; ++i) {
+            const nsRegionRect &rect = rects->mRects[i];
+            printf("  x=%d y=%d width=%u height=%u\n",
+                   rect.x, rect.y, rect.width, rect.height);
+          }
+
+          region->FreeRects(rects);
+        }
+#endif
         
         if (region->IsEmpty())
           break;
 
         // Refresh the view
         if (IsRefreshEnabled()) {
           // If an ancestor widget was hidden and then shown, we could
           // have a delayed resize to handle.
