Add fixme coment about stroke-dasharray implementation.

diff --git a/layout/style/nsRuleNode.cpp b/layout/style/nsRuleNode.cpp
--- a/layout/style/nsRuleNode.cpp
+++ b/layout/style/nsRuleNode.cpp
@@ -4391,6 +4391,7 @@ nsRuleNode::ComputeSVGData(nsStyleStruct
   if (list) {
     if (eCSSUnit_Inherit == list->mValue.GetUnit()) {
       // only do the copy if weren't already set up by the copy constructor
+      // FIXME: This does the wrong thing if aStartStruct is non-null!
       if (!svg->mStrokeDasharray) {
         inherited = PR_TRUE;
         svg->mStrokeDasharrayLength = parentSVG->mStrokeDasharrayLength;
