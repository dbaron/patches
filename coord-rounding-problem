Comment about coordinate rounding problem.

diff --git a/view/src/nsViewManager.cpp b/view/src/nsViewManager.cpp
--- a/view/src/nsViewManager.cpp
+++ b/view/src/nsViewManager.cpp
@@ -1960,6 +1960,7 @@ void nsViewManager::ViewToWidget(nsView 
   aRect += aView->ViewToWidgetOffset();
 
   // finally, convert to device coordinates.
+  // XXXldb This can round pixel-rounded coordinates out an extra pixel!
   aRect.ScaleRoundOut(1.0f / mContext->AppUnitsPerDevPixel());
 }
 
