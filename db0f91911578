
# HG changeset patch
# User Kartikaya Gupta <kgupta@mozilla.com>
# Date 1434638710 14400
# Node ID db0f91911578db121cd5d5e26ad13ccabb1f7a79
# Parent  8eb956e0ce98d86a9df7c8ef63177b532e75202e
Bug 1175631 - Remove an assertion because it causes (at least) marionette failures on OS X 10.6. r=me on a CLOSED TREE

diff --git a/dom/ipc/TabParent.cpp b/dom/ipc/TabParent.cpp
--- a/dom/ipc/TabParent.cpp
+++ b/dom/ipc/TabParent.cpp
@@ -3010,18 +3010,16 @@ TabParent::DeallocPPluginWidgetParent(mo
 {
   delete aActor;
   return true;
 }
 
 nsresult
 TabParent::HandleEvent(nsIDOMEvent* aEvent)
 {
-  MOZ_ASSERT(mFrameElement);
-
   nsAutoString eventType;
   aEvent->GetType(eventType);
 
   if (eventType.EqualsLiteral("MozUpdateWindowPos") && !mIsDestroyed) {
     // This event is sent when the widget moved.  Therefore we only update
     // the position.
     return UpdatePosition();
   }
