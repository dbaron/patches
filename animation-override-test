From: L. David Baron <dbaron@dbaron.org>

Bug 847287 patch N - Add test.

diff --git a/layout/style/test/test_animations_omta.html b/layout/style/test/test_animations_omta.html
--- a/layout/style/test/test_animations_omta.html
+++ b/layout/style/test/test_animations_omta.html
@@ -2093,10 +2093,15 @@ addAsyncAnimTest(function *() {
           "opacity animation overriding transition at 0s");
   advance_clock(500);
   yield waitForPaintsFlushed();
   omta_is("opacity", 0.35, RunningOn.Compositor,
           "opacity animation overriding transition at 0.5s");
   done_div();
 });
 
+// FIXME: Add a test for an overriding animation ending and falling back to
+// another animation, with one where there's a restyle and one where there
+// isn't!  (And also, of course, simpler ones where there's just an
+// animation overridden by something !important.)
+
 </script>
 </html>
