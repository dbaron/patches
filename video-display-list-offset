Fix an off-by-border-and-padding error in nsVideoFrame::BuildDisplayList.  (Bug 470596)

diff --git a/layout/generic/nsVideoFrame.cpp b/layout/generic/nsVideoFrame.cpp
--- a/layout/generic/nsVideoFrame.cpp
+++ b/layout/generic/nsVideoFrame.cpp
@@ -222,17 +222,18 @@ nsVideoFrame::BuildDisplayList(nsDisplay
   NS_ENSURE_SUCCESS(rv, rv);
 
   if (HasVideoData()) {
     rv = aLists.Content()->AppendNewToTop(new (aBuilder) nsDisplayGeneric(this, ::PaintVideo, "Video"));
     NS_ENSURE_SUCCESS(rv, rv);
   }
 
   if (mFrames.FirstChild()) {
-    rv = mFrames.FirstChild()->BuildDisplayListForStackingContext(aBuilder, aDirtyRect, aLists.Content());
+    rv = BuildDisplayListForChild(aBuilder, mFrames.FirstChild(),
+                                  aDirtyRect, aLists.Content());
   }
   return rv;
 }
 
 nsIAtom*
 nsVideoFrame::GetType() const
 {
   return nsGkAtoms::HTMLVideoFrame;
