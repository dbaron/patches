Disable the assertion in GetUsedPadding when we're getting the frame for an event.

diff --git a/layout/base/nsIPresShell.h b/layout/base/nsIPresShell.h
--- a/layout/base/nsIPresShell.h
+++ b/layout/base/nsIPresShell.h
@@ -778,6 +778,10 @@ protected:
   PRPackedBool              mDidInitialReflow;
   PRPackedBool              mIsDestroying;
 
+#ifdef DEBUG
+  PRPackedBool              mGettingFrameForEvent;
+#endif
+
 #ifdef ACCESSIBILITY
   /**
    * Call this when there have been significant changes in the rendering for
