From: L. David Baron <dbaron@dbaron.org>

Don't mark floats as overflow-incomplete in SplitFloat.  (Bug 563584)  (FIXME: Write patch if needed)

diff --git a/layout/generic/nsBlockFrame.cpp b/layout/generic/nsBlockFrame.cpp
--- a/layout/generic/nsBlockFrame.cpp
+++ b/layout/generic/nsBlockFrame.cpp
@@ -3926,16 +3926,22 @@ nsBlockFrame::SplitFloat(nsBlockReflowSt
     if (oldParent != this) {
       ReparentFrame(nextInFlow, oldParent, this);
     }
   } else {
     nsresult rv = aState.mPresContext->PresShell()->FrameConstructor()->
       CreateContinuingFrame(aState.mPresContext, aFloat, this, &nextInFlow);
     NS_ENSURE_SUCCESS(rv, rv);
   }
+  // XXXldb Why do we have to set this here?  It seems like it should
+  // have been set a while ago.  (This is to handle the float itself
+  // having been split only because of overflow inside of it, right,
+  // i.e., nested overflowing floats?)  Or is this marking a frame other
+  // than the one intended?
+  // FIXME: remove aFloatStatus parameter
   if (NS_FRAME_OVERFLOW_IS_INCOMPLETE(aFloatStatus))
     aFloat->GetNextInFlow()->AddStateBits(NS_FRAME_IS_OVERFLOW_CONTAINER);
 
   // The containing block is now overflow-incomplete.
   NS_FRAME_SET_OVERFLOW_INCOMPLETE(aState.mReflowStatus);
 
   if (aFloat->GetStyleDisplay()->mFloats == NS_STYLE_FLOAT_LEFT) {
     aState.mFloatManager->SetSplitLeftFloatAcrossBreak();
