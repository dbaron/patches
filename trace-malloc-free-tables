Free trace-malloc static data structures on shutdown.

diff --git a/tools/trace-malloc/lib/nsTraceMalloc.c b/tools/trace-malloc/lib/nsTraceMalloc.c
--- a/tools/trace-malloc/lib/nsTraceMalloc.c
+++ b/tools/trace-malloc/lib/nsTraceMalloc.c
@@ -489,6 +489,7 @@ static void lfdset_freeentry(void *pool,
     if (flag != HT_FREE_ENTRY)
         return;
     le = (lfdset_entry*) he;
+    __libc_free((char*) le->key);
     __libc_free((void*) le);
 }
 
@@ -1468,6 +1469,7 @@ PR_IMPLEMENT(void) NS_TraceMallocShutdow
             free((void*) fp);
         }
     }
+    /* XXX destroy the libraries, filenames, and methods tables */
     if (tmmon) {
         PRMonitor *mon = tmmon;
         tmmon = NULL;
