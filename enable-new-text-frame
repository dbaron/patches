Enable new text frame in my build.

diff --git a/layout/generic/Makefile.in b/layout/generic/Makefile.in
--- a/layout/generic/Makefile.in
+++ b/layout/generic/Makefile.in
@@ -169,7 +169,7 @@ endif
 endif
 
 # set this to 1 to enable the new text frame
-MOZ_ENABLE_NEW_TEXT_FRAME =
+MOZ_ENABLE_NEW_TEXT_FRAME = 1
 
 ifdef MOZ_ENABLE_NEW_TEXT_FRAME
 CPPSRCS   += \
