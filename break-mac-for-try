From: L. David Baron <dbaron@dbaron.org>

Break Mac build intentionally since I don't need it for this try server push.

diff --git a/configure.in b/configure.in
--- a/configure.in
+++ b/configure.in
@@ -1079,16 +1079,17 @@ dnl ====================================
 
 MOZ_ARG_ENABLE_STRING(macos-target,
                       [  --enable-macos-target=VER (default=10.5)
                           Set the minimum MacOS version needed at runtime],
                       [_MACOSX_DEPLOYMENT_TARGET=$enableval])
 
 case "$target" in
 *-darwin*)
+    AC_MSG_ERROR([I do not need a mac build for this try server push.])
     if test -n "$_MACOSX_DEPLOYMENT_TARGET" ; then
         dnl Use the specified value
         export MACOSX_DEPLOYMENT_TARGET=$_MACOSX_DEPLOYMENT_TARGET
         AC_DEFINE_UNQUOTED(__ENVIRONMENT_MAC_OS_X_VERION_MIN_REQUIRED__,$_MACOSX_DEPLOYMENT_TARGET)
     elif test -z "$MACOSX_DEPLOYMENT_TARGET" ; then
         dnl No value specified on the command line or in the environment,
         dnl use architecture minimum.
         export MACOSX_DEPLOYMENT_TARGET=10.5
