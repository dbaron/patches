From: L. David Baron <dbaron@dbaron.org>

Bug 783133:  Weaken NS_ABORT_IF_FALSE to NS_ASSERTION since it fires somewhat regularly.

diff --git a/netwerk/protocol/websocket/WebSocketChannel.cpp b/netwerk/protocol/websocket/WebSocketChannel.cpp
--- a/netwerk/protocol/websocket/WebSocketChannel.cpp
+++ b/netwerk/protocol/websocket/WebSocketChannel.cpp
@@ -3121,18 +3121,18 @@ WebSocketChannel::OnInputStreamReady(nsI
 
     if (count == 0) {
       mTCPClosed = true;
       AbortSession(NS_BASE_STREAM_CLOSED);
       return NS_OK;
     }
 
     if (mStopped) {
-      NS_ABORT_IF_FALSE(mLingeringCloseTimer,
-                        "OnInputReady after stop without linger");
+      NS_ASSERTION(mLingeringCloseTimer,
+                   "OnInputReady after stop without linger");
       continue;
     }
 
     if (mInflateReader) {
       mInflateStream->ShareData(buffer, count);
       rv = mInflateReader->OnDataAvailable(nullptr, mSocketIn, mInflateStream, 
                                            0, count);
     } else {
