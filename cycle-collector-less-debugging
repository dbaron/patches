Comment out some cycle collector + XPConnect debugging code.  b=374872

diff --git a/js/src/xpconnect/src/nsXPConnect.cpp b/js/src/xpconnect/src/nsXPConnect.cpp
--- a/js/src/xpconnect/src/nsXPConnect.cpp
+++ b/js/src/xpconnect/src/nsXPConnect.cpp
@@ -601,6 +601,7 @@ nsresult
 nsresult 
 nsXPConnect::Traverse(void *p, nsCycleCollectionTraversalCallback &cb)
 {
+    // XXXldb Allocate and free in BeginCycleCollection/EndCycleCollection?
     XPCCallContext cx(NATIVE_CALLER);
 
     PRUint32 refcount = 0;
@@ -610,7 +611,7 @@ nsXPConnect::Traverse(void *p, nsCycleCo
     JSObject *obj = NS_STATIC_CAST(JSObject*, p);
     JSClass* clazz = OBJ_GET_CLASS(cx, obj);
 
-#ifdef DEBUG
+#ifdef DEBUG_xpc_hacker
     char name[72];
     if(XPCNativeWrapper::IsNativeWrapperClass(clazz))
     {
