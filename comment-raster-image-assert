From: L. David Baron <dbaron@dbaron.org>

NOT FOR CHECKIN: Comment out RasterImage assert to make debug builds usable.

I hit this assertion:
 * once on the flickr Uploader
 * once on expensify

diff --git a/image/src/RasterImage.cpp b/image/src/RasterImage.cpp
--- a/image/src/RasterImage.cpp
+++ b/image/src/RasterImage.cpp
@@ -190,17 +190,17 @@ public:
                const nsIntSize& aSize,
                imgFrame* aSrcFrame)
     : dstSize(aSize)
     , dstLocked(false)
     , done(false)
     , stopped(false)
   {
     MOZ_ASSERT(!aSrcFrame->GetIsPaletted());
-    MOZ_ASSERT(aSize.width > 0 && aSize.height > 0);
+    //MOZ_ASSERT(aSize.width > 0 && aSize.height > 0);
 
     weakImage = aImage;
     srcRect = aSrcFrame->GetRect();
   }
 
   // This can only be called on the main thread.
   bool GetSurfaces(imgFrame* srcFrame)
   {
